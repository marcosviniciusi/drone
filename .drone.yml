---
kind: pipeline
type: kubernetes
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: test
  image: alpine:3.8
  commands:
  - apk add curl
  - sleep 3600
  - curl http://localhost:9200

services:
- name: database
  image: elasticsearch:5-alpine
  network_mode: host
  privileged: true
