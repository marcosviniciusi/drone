kind: pipeline
type: exec
name: test
steps:
  - name: dummy-echo
    commands:
      - echo "build step taking 10s. on Branch master"
      - sleep 30
---
kind: pipeline
type: exec
name: perf
steps:
  - name: dummy-echo
    commands:
      - echo "build step taking 10s. on Branch master"
      - sleep 30
